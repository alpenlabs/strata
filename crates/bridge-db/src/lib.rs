//! A layer of abstraction over the actual database.
//!
//! Provides high-level methods to interact with the bridge-related databases/tables. Acts as the
//! single entrypoint via which all other components may interact with the persistence layer.
