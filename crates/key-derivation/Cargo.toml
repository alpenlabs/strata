[package]
name = "strata-key-derivation"
version = "0.1.0"
edition = "2021"

[dependencies]
bitcoin = { workspace = true }
secp256k1 = { workspace = true, features = ["global-context"] }
thiserror.workspace = true
zeroize = { workspace = true, optional = true }

[features]
default = ["zeroize"]
zeroize = ["zeroize/derive"]
