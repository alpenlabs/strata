// use zkvm_primitives::SP1RethInput;

#[cfg(test)]
mod tests {
    // use revm::InMemoryDB;
    use std::fs::File;

    use super::*;
    // use std::fs::File;

    #[test]
    fn test() {
        // let file = File::open("1.bin").expect("file");
        // let mut input: SP1RethInput = bincode::deserialize_from(file).expect("failed");

        // let db = InMemoryDB::initialize(&mut input);

        // println!("it worked");
    }
}
