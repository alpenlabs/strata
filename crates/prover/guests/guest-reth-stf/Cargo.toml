[package]
name = "guest-reth-stf"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
risc0-zkvm = { version = "1.0.2", default-features = false, features = ['std'] }
bincode = { version = "1.3.3" }
revm = { version = "12.1.0", default-features = false }
zkvm-primitives = { git = "https://github.com/MdTeach/reth", branch = "prover-primitives" }
