[package]
name = "sp1-guest-builder"
version = "0.1.0"
edition = "2021"

[build-dependencies]
sp1-helper = "1.0.1"

[package.metadata.risc0]
methods = ["guest-reth-stf"]

[features]
default = []
prover = []
